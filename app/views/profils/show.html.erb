<h1>Profils#index</h1>
<p>Voici la page de <%= @user.email %></p>
<h3>Changer d'avatar ?</h3> 

    <p>Bienvenue sur la page d'ajout d'un avatar pour le User portant l'id : <%=@user.id%></p>
    <h3>Avatar actuel</h3>
    <%if @user.avatar.attached?%>
    <%= link_to "Supprimer le Gossip", profil_avatar_path(@user), method: :delete, class: "btn btn-danger", data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %> 
      <%= image_tag @user.avatar, alt: 'avatar' %>
    <%else%>
      <p>=== Il n'y a pas encore d'avatar lié à cet utilisateur ===</p>
      <%= form_tag profil_avatars_path(@user), multipart: true do %>
      <%= file_field_tag :avatar %>
      <%= submit_tag "mettre à jour" %>
      <% end %>
    <%end%>
    